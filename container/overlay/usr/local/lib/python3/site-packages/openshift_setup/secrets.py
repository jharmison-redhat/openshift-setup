# TODO: Reimplement hack shell script for encryption as pure python
from pathlib import Path

from .utils import shellw


def encrypt(secrets_file: Path) -> str:
    return shellw(f"/bin/bash -c 'source hack/common.sh && encrypt {secrets_file}'")
