{{- range $name, $gwc := .Values.gatewayClasses }}
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: {{ $name }}
spec:
  controllerName: openshift.io/gateway-controller/v1
{{- end }}
