{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "hermes.serviceAccountName" . }}
  labels:
    {{- include "hermes.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.oauth.enabled }}
    serviceaccounts.openshift.io/oauth-redirectreference.primary: '{"kind":"OAuthRedirectReference","apiVersion":"v1","reference":{"kind":"Route","name":"{{ include "hermes.fullname" . }}"}}'
    {{- end }}
automountServiceAccountToken: {{ or .Values.serviceAccount.automount .Values.oauth.enabled }}
{{- end }}
