---
channel: stable-v1.1

config:
  gangScheduling:
    byWorkload:
      admission: Sequential
    policy: ByWorkload
  integrations:
    frameworks:
      - BatchJob
      - RayJob
      - RayCluster
      - PyTorchJob
      - Pod
      - Deployment
      - StatefulSet
      - LeaderWorkerSet
  preemption:
    preemptionPolicy: FairSharing
  workloadManagement:
    labelPolicy: ""

logLevel: Normal
operatorLogLevel: Normal
