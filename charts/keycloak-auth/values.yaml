channel: stable-v26.2

name: keycloak
instances: 1
realm:
  name: sso
  openshiftClientId: ocp-idp
  openshiftClientSecret: abcdefghijkl
  admin:
    username: admin
    password: openshift
  user:
    prefix: user
    count: 5
    password: openshift

removeKubeAdmin: true
openshiftToolsImage: image-registry.openshift-image-registry.svc:5000/openshift/tools:latest

cluster:
  name: cluster
  baseDomain: example.com

cloudnative-pg:
  enabled: true
  clusters:
    - name: keycloak
